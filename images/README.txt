Put the 60 product photographs in this folder.

Keep the filenames exactly as they download from City Chain, for example
HDB001K1_front.jpg — the site already knows those names.

Then open assets/app.js and change the line near the top to:
    const IMAGE_SOURCE = 'local';

Leave it as 'illustration' to run without any photographs.
